# Web explainer

The web application will provide an interactive, step-through view of each runtime trace.

It must visualize the same trace data used by the video renderer. Hand-authored animations that can diverge from backend behavior are out of scope.
