# Video

This application will turn runtime traces into short Remotion compositions.

Each composition should:

- answer one engineering question;
- show the failure before the fix;
- reuse the shared trace and visual primitives;
- expose timing and labels as parameters;
- remain understandable without narration.

The video is a distribution surface. It is not the source of truth for system behavior.
